#include "Point.hpp"

float	calculateAera(Point const a, Point const b, Point const c) {
	
}

bool bsp(Point const a, Point const b, Point const c, Point const point) {
	/*calculate aera of triangle
		area = 1/2(ab + ac + bc) */

	#calculate area of sub triangles

	#if sum of all sub triangles == area of triangle
		#return True
	#else
		#return False
}